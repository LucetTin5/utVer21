mixin video(info)
  .videoBlock
    h4
      a(href=`videos/${info.id}`) #{ info.title.charAt(0).toUpperCase() + info.title.slice(1) }
    p #{ info.description.charAt(0).toUpperCase() + info.description.slice(1) }
    small= info.createdAt
    br
    small= info.tags.join(" ")
    hr
