header
  h1 #{ pageTitle.charAt(0).toUpperCase() + pageTitle.slice(1) }
  nav
    ul
      li
        a(href='/') Home
      li
        a(href='/videos/upload') Upload Video
      li
        a(href='/search') Search
    ul
      if !loggedIn
        li 
          a(href='/join') Join
        li
          a(href='/login') Login
      else
        li
          a(href=`/user/${loggedInUser._id}`) My Profile
        li 
          a(href='/user/logout') Log Out
